# Contributing

Open an issue describing the problem and proposed change, then submit a focused PR. Run npm ci, npm run lint, npm run typecheck, npm test, npm run build, and npm run verify:product. Gallery changes also require consumer and browser checks documented in docs/TESTING.md. Include browser and accessibility evidence for UI changes. Existing commercial terms apply; no open-source license is implied. Do not commit build output or credentials.
